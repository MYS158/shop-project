# ======================================================
# 🗄️ Database Connection Configuration
# ======================================================

# JDBC driver and connection URL
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/shop?useSSL=false&serverTimezone=UTC

# Database credentials
db.username=test
db.password=1234

# Connection pool settings (optional)
db.pool.size=10

# ======================================================
# 🧩 Notes:
# - Ensure the database 'shop' exists before running.
# - The user 'test' must have privileges on 'shop'.
# - File should be excluded from version control (.gitignore).
# ======================================================
